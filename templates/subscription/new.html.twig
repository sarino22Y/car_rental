{% extends 'base.html.twig' %}
{% block title %}Nouvel Abonnement{% endblock %}
{% block body %}
    <div class="container mx-auto px-4">
        <h1 class="text-2xl font-bold text-center my-6">Nouvel Abonnement</h1>
        {{ form_start(form, {'attr': {'class': 'space-y-4'}}) }}
            {{ form_row(form.startDate, {'attr': {'class': 'border rounded p-2 w-full'}}) }}
            {{ form_row(form.endDate, {'attr': {'class': 'border rounded p-2 w-full'}}) }}
            {{ form_row(form.car, {'attr': {'class': 'border rounded p-2 w-full'}}) }}
            <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">Créer</button>
        {{ form_end(form) }}
        <a href="{{ path('app_subscription_index') }}" class="inline-block text-blue-500 hover:underline mt-4">Retour</a>
    </div>
{% endblock %}